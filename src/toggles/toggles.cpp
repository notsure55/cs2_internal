#include <toggles/toggles.h>

namespace Toggles {
    namespace Esp {
        bool boxes { false };
        bool skeletons { true };
        bool health { false };
        bool names { false };
    }
}
